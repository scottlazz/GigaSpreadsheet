{
  "name": "gigaspreadsheet",
  "private": true,
  "version": "0.0.0",
  "source": "src/index.html",
  "alias": {
    "packages": "./packages",
    "src": "./src"
  },
  "scripts": {
    "start": "parcel",
    "build": "parcel build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "parcel": "^2.14.0",
    "svgo": "^3.3.2"
  },
  "dependencies": {
    "golden-layout": "^2.6.0"
  }
}
